check process <%= @process_name %> <%= @process_check %>
    start program = "<%= @process_start_cmd %>"
    stop  program = "<%= @process_stop_cmd %>"
<% @conditions.each do |condition| -%>
    if <%= condition[:if_stmt] %> then <%= condition[:then_stmt] %>
<% end -%>
